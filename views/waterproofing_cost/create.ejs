<br/>
<br/>
<h2>Create a new waterproofing primer product</h2>
<p>There are currently
	<%= estimatePartWaterproofings.query.length %> waterproofing primers. </p>
<br/>
<% 
maxID=0; 
for (var i=0; i<estimatePartWaterproofings.query.length; i++) {
    if(estimatePartWaterproofings.query[i]._id > maxID) {
        maxID=estimatePartWaterproofings.query[i]._id;
        }
}
maxSubtotal=0; 
for (var i=0; i<estimatePartWaterproofings.query.length; i++) {
    if(estimatePartWaterproofings.query[i].subtotal > maxSubtotal) {
        maxSubtotal=estimatePartWaterproofings.query[i].subtotal;
        }
}
%>
	<p>The largest ID is
		<%= maxID %>. </p>
	<div class="content">
		<div>
			<form name="estimatePartWaterproofing" method="POST" action="/estimatePartWaterproofing/save" + _id.value>

				<div class="form-group" id=frm>
					<label for="_id">ID</label>
					<input type="number" class="form-control" id="_id" name="_id" value="<%= maxID+1 %>" required readonly>
				</div>

				<div class="form-group">
					<label for="productType">Product Type</label>
					<input type="text" class="form-control" id="productType" name="productType" value="Urethane" required>
				</div>

				<div class="form-group">
					<label for="ssesUrethane">UsesUrethane</label>
					<input type="text" class="form-control" id="ssesUrethane" name="usesUrethane" value="false" required>
				</div>

				<div class="form-group">
					<label for="subtotal">Sub Total</label>
					<input type="number" class="form-control" id="subtotal" name="subtotal" value="0" required>
				</div>
				<div class="form-group">
					<input type="submit" value="Post new entry" class="btn btn-primary" />
				</div>
			</form>
			<br/>
			<form method="get" action="/estimatePartWaterproofing">
				<input type="submit" value="Return to list" class="btn btn-caution" />
			</form>
		</div>
	</div>

	<!-- These Views are managed by Team 04-07 -->
<!-- KamalaPriya Bharam -->
<!-- Sirisha, Vanamali -->
