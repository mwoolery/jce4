
<% console.log(estimatePartLabors.query); %>
<div class="container">
 <h2>All entries</h2>

<p>There are  <%= estimatePartLabors.query.length %> entries. <br/>
<a href="create.ejs">Add a new one.</a></p>

<table class="table" border="1">
    <thead>
      <tr>
        <th>Subtotal</th>
       <th>Actions</th>
    </tr>
    </thead>
    <tbody>
<% for (var i = 0; i < estimatePartLabors.query.length; i++) { %>
         <tr>
            <td><%=  estimatePartLabors.query[i].subtotal %></td>
            <td> <a href="/estimatePartLabor/delete/<%= i+1 %>">Delete</a> 
            <a href="/estimatePartLabor/delete/<%= i+1 %>"> Details </a>
            <a href="/estimatePartLabor/delete/<%= i+1 %>"> Edit</a>
            </td>
         </tr>
         <% } %>
</tbody>
</table>
</div>
