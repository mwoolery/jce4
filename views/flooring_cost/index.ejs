<% console.log(estimatePartFloorings.query); %>
<div class="container">
<h2>All entries</h2>

<p>There are  <%= estimatePartFloorings.query.length %> entries. <br/>
<a href="estimatePartFlooring/create">Add a new one.</a></p>

<table class="table" border="1">
    <thead>
      <tr>
            <th>ID</th>
			<th>Floor System Type</th>
			<th>Uses Urethane</th>
			<th>Urethane Product Selection</th>
            <th>Urethane Coverage SqFt</th>
			<th>Uses Epoxy</th>
            <th>Epoxy Product Selection</th>
            <th>Epoxy Coverage SqFt</th>
			<th>Subtotal</th>
       <th>Actions</th>
    </tr>
    </thead>
    <tbody>
<% for (var i = 0; i < estimatePartFloorings.query.length; i++) { %>
         <tr>
                        <td>
							<%=  estimatePartFloorings.query[i]._id %>
						</td>
						<td>
							<%=  estimatePartFloorings.query[i].floorSystemType %>
						</td>
						<td>
							<%=  estimatePartFloorings.query[i].usesUrethane %>
						</td>
						<td>
							<%=  estimatePartFloorings.query[i].urethaneProductSelection %>
						</td>
						<td>
							<%=  estimatePartFloorings.query[i].urethaneCoverageSqFt %>
						</td>
                        <td>
							<%=  estimatePartFloorings.query[i].usesEpoxy %>
						</td>
						<td>
							<%=  estimatePartFloorings.query[i].expoxyProductSelection %>
						</td>
                        <td>
							<%=  estimatePartFloorings.query[i].expoxyCoverageSqFt %>
						</td>
                        <td>
                            <% 
                                var sqft = 2000;
                                var urethaneCost = 0;
                                var epoxyCost = 0;
                                var subTotal = urethaneCost  + epoxyCost;
                            %>
                            <%=  subTotal %>
                        </td>

            <td>
                <a href="estimatePartFlooring/delete/<%=  estimatePartFloorings.query[i]._id %>"> <button type="button" class="btn btn-danger"> <span class="glyphicon glyphicon-trash"></span>Delete</button></a> <br/><br/>
                <a href="estimatePartFlooring/details/<%= estimatePartFloorings.query[i]._id %>"> <button type="button" class="btn btn-info"> <span class="glyphicon glyphicon-list-alt">Details</button> </a> <br/><br/>
                <a href="estimatePartFlooring/edit/<%=    estimatePartFloorings.query[i]._id %>"> <button type="button" class="btn btn-warning"><span class="glyphicon glyphicon-edit"></span> Edit</button> </a>
            </td>
         </tr>
         <% } %>
</tbody>
</table>
</div>

<!-- This model is managed by Team 4-5
     Chaitanya Kiran Moturu
     Manikanteswara Rao Earla
     Sainath Gulla -->
