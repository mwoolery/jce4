<br/>
<br/>
<h2>Create a new estimate part flooring product</h2>
<p>There are currently
	<%= estimatePartFloorings.query.length %> Estimate part flooring. </p>
<br/>
<% 
maxID=0; 
for (var i=0; i<estimatePartFloorings.query.length; i++) {
    if(estimatePartFloorings.query[i]._id > maxID) {
        maxID=estimatePartFloorings.query[i]._id;
        }
}
%>
	<p>The largest ID is
		<%= maxID %>. </p>
	
	<div class="content">
		<div>
			<form name="estimatePartFlooring" method="POST" action="/estimatePartFlooring/save" + _id.value>

				<div class="form-group" id=frm>
					<label for="_id">ID</label>
					<input type="number" class="form-control" id="_id" name="_id" value="<%= maxID+1 %>" required readonly>
				</div>

				

			<!--	<div class="form-group">
					<label for="subtotal">Sub Total</label>
					<input type="number" class="form-control" id="subtotal" name="subtotal" value="0" required>
					<% 
                        var sqft = 2000;
                        var urethaneCost = 0;
                        var epoxyCost = 0;
                        var subTotal = urethaneCost  + epoxyCost;
                    %>
                    <%=  subTotal %>
				</div> -->

				<% include ..\estimateparts\est-flooring %>

				<div class="form-group">
					<input type="submit" value="Post new entry" class="btn btn-primary" />
				</div>
			</form>
			<br/>
			<form method="get" action="/estimatePartFlooring">
				<input type="submit" value="Return to list" class="btn btn-caution" />
			</form>
		</div>
	</div>
<script src="../js/estimatePartFlooring.js"></script>


<!-- This model is managed by Team 4-5
     Chaitanya Kiran Moturu
     Manikanteswara Rao Earla
     Sainath Gulla --> 
