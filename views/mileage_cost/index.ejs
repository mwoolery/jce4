
<% console.log(estimatePartMileages.query); %>
<div class="container">
 <h2>All entries</h2>

<p>There are  <%= estimatePartMileages.query.length %> entries. <br/>
<a href="estimatePartMileage/create">Add a new one.</a></p>

<table class="table" border="1">
    <thead>
      <tr>
        <th>Subtotal</th>
       <th>Actions</th>
    </tr>
    </thead>
    <tbody>
<% for (var i = 0; i < estimatePartMileages.query.length; i++) { %>
         <tr>
            <td><%=  estimatePartMileages.query[i].subtotal %></td>
            <td><a href="estimatePartMileage/delete/<%= i+1 %>"> Delete</a> | <a href="estimatePartMileage/details/<%= i+1 %>"> Details</a> | <a href="estimatePartMileage/edit/<%= i+1 %>"> Edit</a></td>
         </tr>
         <% } %>
</tbody>
</table>
</div>

<!-- This model is managed by Team 4-11 --> 
<!--  Nuli Kartik -->
<!-- Bapuji Dirisala-->

